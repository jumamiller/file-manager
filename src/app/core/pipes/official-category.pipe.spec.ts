import { OfficialCategoryPipe } from './official-category.pipe';

describe('OfficialCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new OfficialCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
